language: java

script: 
  - cd sdk ; mvn test
  - echo $test
  
jdk:
  - oraclejdk7
  - openjdk7

env:
  global:
    - DOCKER_COMPOSE_VERSION=1.5.0rc3
  
matrix:
  exclude:
    - jdk: openjdk7
      env: DOCKER_COMPOSE_VERSION=1.5.0rc3
